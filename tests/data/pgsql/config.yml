parameters:
    db:
        host: pgsql
        port: 5432
        database: postgres
        user: postgres
        password: mysecretpassword

    tables:
        -
            id: 0
            name: escaping
            query: "SELECT * FROM escaping"
            outputTable: in.c-main.escaping
            incremental: false
            primaryKey: ~
            enabled: true
